# 📦 DARV - التثبيت الكامل

هذا الملف يحتوي على جميع الملفات المطلوبة لتشغيل DARV.

---

## 🏗️ الهيكل المطلوب

```
darv/
├── core/
│   ├── darv_types.hpp
│   ├── darv_executor.hpp
│   ├── darv_evaluator.hpp
│   ├── darv_improver.hpp
│   └── darv_cycle.hpp
├── projects/
│   └── example_project/
│       ├── main.cpp
│       └── CMakeLists.txt
├── main.cpp
├── CMakeLists.txt
└── build.sh
```

---

## 📝 الخطوات

### 1. إنشاء الهيكل الأساسي

```bash
mkdir -p darv/{core,projects/example_project,memory/history}
cd darv
```

### 2. نسخ جميع الملفات

استخدم الملفات من الـ Artifacts السابقة:

1. **core/darv_types.hpp** - من artifact `darv_types`
2. **core/darv_executor.hpp** - من artifact `darv_executor`
3. **core/darv_evaluator.hpp** - من artifact `darv_evaluator`
4. **core/darv_improver.hpp** - من artifact `darv_improver`
5. **core/darv_cycle.hpp** - من artifact `darv_cycle`
6. **main.cpp** - من artifact `darv_main`
7. **CMakeLists.txt** - من artifact `root_cmake`
8. **projects/example_project/main.cpp** - من artifact `example_project`
9. **projects/example_project/CMakeLists.txt** - من artifact `example_cmake`
10. **build.sh** - من artifact `build_script`

### 3. جعل السكريبت قابل للتنفيذ

```bash
chmod +x build.sh
```

### 4. التشغيل

```bash
./build.sh
```

---

## ✅ التحقق من التثبيت

بعد التشغيل، يجب أن ترى:

```
✓ تم إنشاء المجلدات
✓ تم إنشاء المشروع المثال
✓ تم بناء DARV بنجاح
✓ تم بناء المشروع المثال
```

ثم يبدأ DARV في التنفيذ ويعرض الدورات.

---

## 📋 قائمة المراجعة

- [ ] تم إنشاء مجلد `core/`
- [ ] تم نسخ جميع ملفات `.hpp`
- [ ] تم إنشاء `projects/example_project/`
- [ ] تم نسخ ملفات المثال
- [ ] تم نسخ `main.cpp` الرئيسي
- [ ] تم نسخ `CMakeLists.txt` الرئيسي
- [ ] تم نسخ `build.sh`
- [ ] تم تشغيل `chmod +x build.sh`
- [ ] تم تشغيل `./build.sh`

---

## 🎯 ملخص الملفات

| الملف | الحجم التقريبي | الوصف |
|-------|----------------|-------|
| `darv_types.hpp` | ~100 سطر | تعريفات الأنواع والهياكل |
| `darv_executor.hpp` | ~120 سطر | محرك التنفيذ |
| `darv_evaluator.hpp` | ~150 سطر | محلل التقييم |
| `darv_improver.hpp` | ~180 سطر | مولد التحسينات |
| `darv_cycle.hpp` | ~220 سطر | محرك الدورة الكاملة |
| `main.cpp` | ~50 سطر | البرنامج الرئيسي |
| `CMakeLists.txt` (root) | ~15 سطر | إعدادات البناء |
| `example/main.cpp` | ~20 سطر | المشروع المثال |
| `example/CMakeLists.txt` | ~10 سطر | إعدادات المثال |
| `build.sh` | ~80 سطر | سكريبت البناء |

**الإجمالي**: ~950 سطر من الكود

---

## 🚀 البدء السريع

إذا كنت تريد البدء مباشرة بدون فهم التفاصيل:

```bash
# 1. إنشاء المشروع
mkdir darv && cd darv

# 2. نسخ جميع الملفات (من Artifacts)

# 3. تشغيل
chmod +x build.sh && ./build.sh
```

هذا كل شيء! 🎉

---

## 📚 التوثيق

- **README.md** - التوثيق الكامل
- **QUICKSTART.md** - دليل البدء السريع
- **CONCEPT.md** - الفلسفة والرؤية

---

## 💡 نصيحة

لأول مرة، ابدأ بالمشروع المثال المقدم. بعد أن تفهم كيف يعمل النظام، يمكنك إضافة مشاريعك الخاصة.

---

## ⚠️ ملاحظات مهمة

1. **المتطلبات**: CMake 3.10+, C++17, GCC/Clang
2. **نظام التشغيل**: Linux/Unix (مختبر على Ubuntu)
3. **النسخة**: v0.1 - نموذج أولي تجريبي

---

## 🆘 الدعم

إذا واجهت مشاكل:
1. تحقق من المتطلبات
2. راجع `QUICKSTART.md`
3. افتح Issue على GitHub
4. راسلنا: contact@sircle.tech

---

**بالتوفيق! 🚀**

**Sircle Technologies**
*Building the future of autonomous software development*