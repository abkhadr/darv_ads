# 🚀 دليل البدء السريع - DARV

## البدء في 5 دقائق

### الخطوة 1: تجهيز الملفات

قم بإنشاء المشروع بالهيكل التالي:

```
darv/
├── core/
│   ├── darv_types.hpp
│   ├── darv_executor.hpp
│   ├── darv_evaluator.hpp
│   ├── darv_improver.hpp
│   └── darv_cycle.hpp
├── projects/
│   └── example_project/
│       ├── main.cpp
│       └── CMakeLists.txt
├── main.cpp
├── CMakeLists.txt
└── build.sh
```

### الخطوة 2: جعل السكريبت قابل للتنفيذ

```bash
chmod +x build.sh
```

### الخطوة 3: التشغيل!

```bash
./build.sh
```

هذا كل شيء! 🎉

---

## ماذا سيحدث؟

1. **البناء الأولي**: سيتم بناء DARV والمشروع المثال
2. **الدورة 1**: تشغيل المشروع بدون optimization (بطيء ~2500ms)
3. **التقييم**: DARV يكتشف أن الأداء بطيء
4. **التحسين**: إضافة `-O2` flag
5. **الدورة 2**: تشغيل المشروع مع optimization (سريع ~450ms) ✓
6. **التقارب**: النظام يتوقف لأن الأداء أصبح ممتاز

---

## النتيجة المتوقعة

```
╔════════════════════════════════════════╗
║   دورة DARV رقم:  1               ║
╚════════════════════════════════════════╝

► الخطوة 1/4: البناء (Build)
[EXECUTOR] تنفيذ: cd ./projects/example_project && mkdir -p build
[EXECUTOR] النتيجة: نجح ✓ (الوقت: 45 ms)

► الخطوة 2/4: التنفيذ (Execute)
[EXECUTOR] تنفيذ: ./projects/example_project/build/example
المجموع: 49999995000000
الوقت المستغرق: 2547.32 ms
[EXECUTOR] النتيجة: نجح ✓ (الوقت: 2550 ms)

► الخطوة 3/4: التقييم (Evaluate)
[EVALUATOR] تقييم النتيجة...
[EVALUATOR] النتيجة: 65/100
[EVALUATOR] الأخطاء: 0
[EVALUATOR] التحذيرات: 0
[EVALUATOR] وقت التنفيذ: 2547.32 ms
[EVALUATOR] يحتاج تحسين: نعم

► الخطوة 4/4: التحسين (Improve)
[IMPROVER] توليد تحسينات...
[IMPROVER] تم توليد 1 تحسين
  - إضافة optimization flag -O2 لتسريع التنفيذ (الأولوية: 8)
[IMPROVER] تطبيق التحسينات...
[IMPROVER] تطبيق: إضافة optimization flag -O2 لتسريع التنفيذ
[IMPROVER] تم التطبيق بنجاح ✓

═══ ملخص الدورة 1 ═══
النتيجة: 65/100
الوقت: 2547.32 ms
الملاحظات: تم تطبيق 1 تحسين

⏳ انتظار قبل الدورة التالية...

╔════════════════════════════════════════╗
║   دورة DARV رقم:  2               ║
╚════════════════════════════════════════╝

[... نفس الخطوات ...]

═══ ملخص الدورة 2 ═══
النتيجة: 92/100
الوقت: 452.18 ms
الملاحظات: لا يوجد تحسينات مطلوبة

✓ تم الوصول للتقارب (Convergence)!
النظام توقف بعد 2 دورة

╔════════════════════════════════════════╗
║       ملخص شامل لجميع الدورات        ║
╚════════════════════════════════════════╝

عدد الدورات المنفذة: 2

دورة 1: النتيجة=65 | الوقت=2547.32ms | التحسينات=1
دورة 2: النتيجة=92 | الوقت=452.18ms | التحسينات=0

═══ الإحصائيات ═══
النتيجة الأولى: 65/100
النتيجة النهائية: 92/100
التحسن الكلي: 27 نقطة

✓ انتهت جميع الدورات بنجاح!
```

---

## 📊 فحص النتائج

### سجل الدورات
```bash
cat memory/cycles.log
```

### التحسينات المطبقة
```bash
cat memory/history/patch_001.diff
```

### المشروع المحسّن
```bash
cat projects/example_project/CMakeLists.txt
```

ستلاحظ أن DARV أضاف تلقائياً:
```cmake
set(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} -O2")
```

---

## 🎯 تجربة مشروع خاص

### 1. إنشاء مشروع جديد

```bash
mkdir -p projects/my_app
cd projects/my_app
```

### 2. إضافة الكود

```cpp
// main.cpp
#include <iostream>
#include <vector>

int main() {
    std::vector<int> data;
    for(int i = 0; i < 1000000; i++) {
        data.push_back(i * 2);
    }
    std::cout << "Size: " << data.size() << std::endl;
    return 0;
}
```

### 3. CMakeLists.txt

```cmake
cmake_minimum_required(VERSION 3.10)
project(my_app)
set(CMAKE_CXX_STANDARD 11)
add_executable(my_app main.cpp)
```

### 4. تحديث main.cpp الرئيسي

غيّر في `main.cpp` الرئيسي:
```cpp
config.name = "my_app";
config.root_path = "./projects/my_app";
config.executable_name = "my_app";
```

### 5. إعادة البناء والتشغيل

```bash
cd ../..  # العودة للجذر
./build.sh
```

---

## ❓ أسئلة شائعة

### س: هل يمكن تغيير عدد الدورات؟
نعم، في `main.cpp`:
```cpp
config.max_cycles = 10;  // بدلاً من 5
```

### س: كيف أجعله أكثر صرامة في التقييم؟
```cpp
config.convergence_threshold = 1.0;  // بدلاً من 2.0
```

### س: هل يمكن إضافة تحسينات مخصصة؟
نعم! عدّل `darv_improver.hpp` وأضف قواعدك الخاصة في:
```cpp
std::vector<Improvement> generate_simple_improvements(...)
```

---

## 🐛 حل المشاكل

### "Permission denied: ./build.sh"
```bash
chmod +x build.sh
```

### "cmake: command not found"
```bash
# Ubuntu/Debian
sudo apt-get install cmake build-essential

# macOS
brew install cmake
```

### "No such file or directory"
تأكد من أنك في مجلد المشروع الصحيح:
```bash
ls -la  # يجب أن ترى: core/, projects/, main.cpp
```

---

## 🎓 الخطوة التالية

بعد تجربة النموذج الأساسي، جرّب:

1. **تعديل المشروع المثال** - جرب كود مختلف
2. **إضافة تحسينات جديدة** - عدّل `darv_improver.hpp`
3. **دمج LLM API** - للتحسينات الذكية
4. **إضافة اختبارات** - لضمان جودة التحسينات

---

## 📚 مصادر إضافية

- [README.md](README.md) - التوثيق الكامل
- [DARV Concept Document](CONCEPT.md) - الفلسفة والرؤية
- [GitHub Issues](https://github.com/your-username/darv/issues) - للإبلاغ عن المشاكل

---

**مبروك! أنت الآن تستخدم DARV - نظام التطوير الذاتي الدائري 🎉**

للمزيد من المساعدة: [contact@sircle.tech](mailto:contact@sircle.tech)