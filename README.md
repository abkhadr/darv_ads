# DARV - نظام التطوير الذاتي الدائري
## Autonomous Circular Runtime

---

## 📋 نظرة عامة

**DARV** هو نظام تجريبي يهدف لبناء محرك تطوير برمجي ذاتي قادر على:
- ✅ تنفيذ المشاريع البرمجية
- 📊 تقييم الأداء والجودة
- 🔧 اقتراح وتطبيق التحسينات
- 🔄 إعادة البناء والتشغيل في دورة مستمرة

---

## 📁 هيكل المشروع

```
darv/
├── core/                      # النواة الأساسية
│   ├── darv_types.hpp        # تعريفات الأنواع
│   ├── darv_executor.hpp     # محرك التنفيذ
│   ├── darv_evaluator.hpp    # محلل التقييم
│   ├── darv_improver.hpp     # مولد التحسينات
│   └── darv_cycle.hpp        # محرك الدورة الكاملة
│
├── projects/                  # المشاريع المدارة
│   └── example_project/
│       ├── main.cpp
│       └── CMakeLists.txt
│
├── memory/                    # ذاكرة النظام
│   ├── cycles.log            # سجل الدورات
│   └── history/              # تاريخ التحسينات
│       ├── patch_001.diff
│       └── patch_002.diff
│
├── main.cpp                   # البرنامج الرئيسي
└── CMakeLists.txt            # نظام البناء
```

---

## 🚀 التثبيت والتشغيل

### المتطلبات

- **C++17** أو أحدث
- **CMake 3.10+**
- **GCC/Clang** مع دعم filesystem
- نظام Linux/Unix (مختبر على Ubuntu)

### خطوات البناء

```bash
# 1. استنساخ المشروع
git clone https://github.com/your-username/darv.git
cd darv

# 2. إنشاء مجلد البناء
mkdir build
cd build

# 3. تشغيل CMake
cmake ..

# 4. البناء
make

# 5. التشغيل
./darv
```

---

## 🎯 كيف يعمل DARV

### الدورة الأساسية (DARV Cycle)

```
┌─────────────┐
│   Execute   │  تنفيذ المشروع
└──────┬──────┘
       │
       ▼
┌─────────────┐
│  Evaluate   │  تقييم النتائج
└──────┬──────┘
       │
       ▼
┌─────────────┐
│   Improve   │  توليد التحسينات
└──────┬──────┘
       │
       ▼
┌─────────────┐
│    Build    │  إعادة البناء
└──────┬──────┘
       │
       └────────► العودة للتنفيذ
```

### مثال على دورة واحدة

**الدورة 1:**
1. **Execute**: تشغيل البرنامج → استغرق 2500ms
2. **Evaluate**: تحليل الأداء → النتيجة 65/100
3. **Improve**: اقتراح إضافة `-O2` flag
4. **Build**: إعادة البناء مع التحسين

**الدورة 2:**
1. **Execute**: تشغيل البرنامج → استغرق 450ms ✓
2. **Evaluate**: تحليل الأداء → النتيجة 92/100 ✓
3. **Improve**: لا يوجد تحسينات جديدة
4. **Convergence**: توقف النظام (وصل للتقارب)

---

## 📊 نظام التقييم

### المقاييس المستخدمة

| المقياس | الوصف | الوزن |
|---------|-------|------|
| **Execution Time** | وقت التنفيذ | عالي |
| **Error Count** | عدد الأخطاء | حرج |
| **Warning Count** | عدد التحذيرات | متوسط |
| **Success Rate** | نسبة النجاح | حرج |

### نظام النقاط

- البداية: **100 نقطة**
- كل خطأ: **-20 نقطة**
- كل تحذير: **-5 نقاط**
- البطء (>1000ms): **-0.01 نقطة/ms**

---

## 🔧 التحسينات المدعومة (النسخة الحالية)

### 1. Optimization Flags
```cmake
set(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} -O2")
```
- **متى**: عندما يكون وقت التنفيذ > 1000ms
- **التأثير المتوقع**: 30-70% تحسن في السرعة

### 2. Warning Flags
```cmake
set(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} -Wall -Wextra")
```
- **متى**: عند وجود تحذيرات
- **التأثير المتوقع**: تحسين جودة الكود

### 3. C++ Standard
```cmake
set(CMAKE_CXX_STANDARD 17)
```
- **متى**: دائماً (إذا لم يكن محدد)
- **التأثير المتوقع**: الوصول لميزات حديثة

---

## 📝 إضافة مشروع جديد

### 1. إنشاء مجلد المشروع

```bash
mkdir -p projects/my_project
cd projects/my_project
```

### 2. إضافة الكود

```cpp
// main.cpp
#include <iostream>

int main() {
    std::cout << "Hello DARV!" << std::endl;
    return 0;
}
```

### 3. إنشاء CMakeLists.txt

```cmake
cmake_minimum_required(VERSION 3.10)
project(my_project)

set(CMAKE_CXX_STANDARD 11)
add_executable(my_project main.cpp)
```

### 4. تحديث الإعدادات في main.cpp

```cpp
ProjectConfig config;
config.name = "my_project";
config.root_path = "./projects/my_project";
// ... باقي الإعدادات
```

---

## 🔍 مثال عملي: تحسين أداء

### الكود الأولي (بطيء)

```cpp
// main.cpp
#include <iostream>
#include <vector>

int main() {
    long long sum = 0;
    for (int i = 0; i < 10000000; i++) {
        sum += i;
    }
    std::cout << "Sum: " << sum << std::endl;
    return 0;
}
```

### CMakeLists.txt الأولي

```cmake
cmake_minimum_required(VERSION 3.10)
project(example)

set(CMAKE_CXX_STANDARD 11)  # بدون optimization
add_executable(example main.cpp)
```

### نتائج DARV

```
دورة 1: النتيجة=65/100 | الوقت=2500ms
  → DARV يضيف -O2 flag

دورة 2: النتيجة=92/100 | الوقت=450ms ✓
  → تحسن بنسبة 82%!

دورة 3: النتيجة=94/100 | الوقت=440ms
  → Convergence: توقف النظام
```

---

## 📈 قراءة السجلات

### ملف cycles.log

```
========================================
دورة رقم: 1
الوقت: Mon Dec  1 10:30:45 2025
النتيجة: 65/100
وقت التنفيذ: 2500 ms
التحسينات المطبقة: 1
ملاحظات: تم تطبيق 1 تحسين
========================================
```

### ملفات Patches

```bash
# memory/history/patch_001.diff
--- CMakeLists.txt
+++ CMakeLists.txt
@@ Improvement: إضافة optimization flag -O2 @@

set(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} -O2")
```

---

## 🎛️ الإعدادات المتقدمة

### ProjectConfig

```cpp
ProjectConfig config;
config.max_cycles = 10;              // الحد الأقصى للدورات
config.convergence_threshold = 2.0;  // عتبة التقارب (نقاط)
```

### معايير التقارب

النظام يتوقف عندما:
1. آخر 3 دورات تحسنت بأقل من `convergence_threshold`
2. النتيجة > 95/100
3. وصل للحد الأقصى من الدورات

---

## 🚧 القيود الحالية

### النسخة الحالية (v0.1)

- ✅ تحسينات بسيطة (Rule-based)
- ❌ لا يدعم LLM/AI حتى الآن
- ❌ لا يدعم multi-file refactoring
- ❌ لا يوجد Sandbox حقيقي (يستخدم نظام التشغيل مباشرة)

### التحسينات المستقبلية

- 🔜 دمج Claude API للتحسينات الذكية
- 🔜 Sandbox حقيقي (Docker/Containers)
- 🔜 تحليل Static Analysis متقدم
- 🔜 دعم multi-language (Python, Rust, etc.)
- 🔜 واجهة ويب لمراقبة الدورات

---

## 🐛 استكشاف الأخطاء

### مشكلة: "cmake: command not found"

```bash
# Ubuntu/Debian
sudo apt-get install cmake

# macOS
brew install cmake
```

### مشكلة: "undefined reference to pthread"

تأكد من وجود هذا السطر في CMakeLists.txt:
```cmake
target_link_libraries(darv Threads::Threads)
```

### مشكلة: "filesystem not found"

```bash
# إضافة flag للكومبايلر
target_link_libraries(darv stdc++fs)
```

---

## 🤝 المساهمة

نرحب بالمساهمات! الخطوات:

1. Fork المشروع
2. إنشاء branch للميزة: `git checkout -b feature/AmazingFeature`
3. Commit التغييرات: `git commit -m 'Add AmazingFeature'`
4. Push للـ branch: `git push origin feature/AmazingFeature`
5. فتح Pull Request

---

## 📜 الترخيص

هذا المشروع مرخص تحت MIT License - انظر ملف [LICENSE](LICENSE) للتفاصيل.

---

## 👥 التواصل

- **المشروع**: [DARV on GitHub](https://github.com/your-username/darv)
- **الشركة**: Sircle Technologies
- **البريد**: contact@sircle.tech

---

## 🙏 شكر خاص

- مستوحى من مفاهيم Self-Modifying Code
- مبني على أفكار Genetic Algorithms
- جزء من رؤية Sircle لبناء أنظمة ذكية ذاتية التطور

---

**صُنع بـ ❤️ من أجل مستقبل البرمجة الذاتية**

**DARV v0.1 - النواة الأولى**
